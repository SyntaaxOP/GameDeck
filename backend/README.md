# GameDeck backend

FastAPI and SQLite backend with a lifecycle-managed Windows process tracker. Run one Uvicorn worker so only one tracker owns session transitions. See the repository-level `README.md` for setup and verification instructions.
